# 源码导入与验证记录

日期：2026-09-20。

本次将已有后端工程与前端工程整理为单仓库，按业务模块分批导入。全部提交使用实际整理时间；模块导入提交不代表原始开发顺序。上游项目说明保留在 [原始 README](upstream-readme.md)。

## 发布整理

- 后端保留在仓库根目录，前端放入 `frontend/`。
- 原有 JWT 密钥文件、数据库/API/OSS 本机凭据、构建产物和 IDE 配置不进入 Git。
- 应用配置使用环境变量，JWT 私钥使用外部文件；测试在内存中生成临时 RSA 密钥。
- Docker Compose 前端构建目录改为 `./frontend`，JWT 密钥通过本地目录只读挂载。
- 各模块提交包含实际源文件；未使用空提交或回填历史日期。

## 本次执行结果

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| Java 编译与单元测试 | JDK 21 下 `mvn -B -Dfile.encoding=UTF-8 test` | 2 项通过、0 失败 |
| 前端依赖安装 | `cd frontend` 后 `npm ci --no-audit --no-fund` | 通过 |
| 前端类型检查与生产构建 | `npm run build` | 通过，344 个模块转换完成 |
| Compose 配置解析 | 仓库根目录执行 `docker compose config --quiet` | 通过 |
| Git 历史检查 | 遍历已提交 blob，检查已知本机凭据、密钥内容和常见令牌格式 | 无匹配；无 PEM、`.env`、`.local`、`target`、`node_modules` 被跟踪 |

Java 测试覆盖 JWT 签发/解析与热点探测。上述结果不等于完整中间件链路、AI 模型服务或 OSS 上传已经联调通过。本次没有启动完整 Docker 服务，也没有调用真实模型。

前端构建提示浏览器兼容性数据过期，构建成功；依赖版本本次保持原锁文件内容。
